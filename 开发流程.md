# 开发流程手册（零基础版）

这份文档定义了「生活工作台」的完整开发与发版流程。
**每次改动都按这个流程走，你的项目就和正规软件公司的一样严谨。**

---

## 一、项目里每个文件夹是干什么的

```
工作台/                    ← 项目根目录 = git 仓库
├── app/                   ← 【源码】你唯一需要改的地方
│   ├── index.html         ← 全部业务代码（界面+逻辑，单文件）
│   ├── AndroidManifest.xml← 版本号、权限等安卓配置
│   ├── java/.../MainActivity.java ← 安卓壳（一般不用动）
│   └── res/               ← 应用图标
├── docs/                  ← 审查报告、历史更新日志存档
├── releases/              ← 每个版本的 APK 存档（勿删，可回滚）
├── keystore/              ← 【签名密钥】被 .gitignore 排除，绝不上传
│   ├── wb.keystore        ← 签名文件，丢了就永远无法覆盖升级！
│   └── pass.txt           ← 签名密码
├── build.sh               ← 一键构建脚本
├── CHANGELOG.md           ← 更新日志（每次改动都要记）
├── README.md              ← 项目门面（GitHub 首页显示的内容）
└── 开发流程.md            ← 本文档
```

**三条铁律：**
1. `keystore/wb.keystore` 丢失 = 所有老用户无法覆盖升级，必须卸载重装丢数据。把它再备份一份到网盘/U盘。
2. `keystore/` 绝不提交到 GitHub（.gitignore 已排除，不要手动 add）。
3. 版本号只能往上涨，绝不复用、绝不回退版本号。

---

## 二、版本号规则（语义化版本）

格式：`主版本.次版本.修订号`，例如 `2.1.3`

| 改动类型 | 升哪一位 | 例子 |
|---|---|---|
| 只修 bug、小优化 | 修订号 +1 | 2.0.0 → 2.0.1 |
| 加新功能（向后兼容） | 次版本 +1，修订归零 | 2.0.1 → 2.1.0 |
| 大改版、不兼容旧数据 | 主版本 +1，其余归零 | 2.1.0 → 3.0.0 |

**两个版本号必须同时改**（都在 `app/AndroidManifest.xml` 开头）：
- `versionName="2.1.0"` ← 给人看的，遵守上面规则
- `versionCode="3"` ← 给安卓看的整数，**每次发版 +1**，只能涨不能降

---

## 三、日常改动流程（增删功能、修 bug）

每次想改东西，按这个顺序：

1. **改代码**：编辑 `app/index.html`（界面和逻辑都在这一个文件里）。
2. **本地预览**：浏览器直接打开 `app/index.html` 就能看效果、测功能。
3. **记日志**：在 `CHANGELOG.md` 顶部的「未发布」段落（没有就自己加一节 `## [未发布]`）写下你改了什么，分类：新增 / 修复 / 优化 / 移除。
4. 攒够一批改动，准备发版（见第四节）。

> 小改动也可以随时提交 git（见第五节），不必等到发版。
> 提交越频繁，历史越清晰，改坏了越容易回退。

---

## 四、发版流程（8 步 Checklist）

当一批改动测试通过，准备发布新版本：

- [ ] **1. 升版本号**：改 `app/AndroidManifest.xml` 的 `versionName`（按规则）和 `versionCode`（+1）。
- [ ] **2. 落日期**：把 `CHANGELOG.md` 里「未发布」改成版本号和当天日期，如 `## [2.1.0] - 2026-08-10`。
- [ ] **3. 构建**：在 Git Bash 里运行 `bash build.sh`，产物自动存到 `releases/LifeWorkbench_v2.1.0.apk`。
- [ ] **4. 装机测试**：传到手机**覆盖安装**（不要卸载！验证数据保留），重点测：本次改动的功能 + 导出备份 + 导入恢复。
- [ ] **5. 提交**：`git add -A && git commit -m "v2.1.0: 一句话说明本次版本内容"`。
- [ ] **6. 打标签**：`git tag -a v2.1.0 -m "版本说明"`。
- [ ] **7. 推送**：`git push && git push --tags`。
- [ ] **8. 发 Release**：在 GitHub 仓库页 → Releases → Draft a new release → 选刚推的标签 → 把 APK 拖进去 → 发布。

> 第 3、5、6、7、8 步以后都可以直接叫 WorkBuddy 帮你做，你说"发版 v2.1.0"就行。

---

## 五、git 常用命令速查（在「工作台」文件夹里右键 → Git Bash）

```bash
git status              # 看看现在有哪些文件被改了
git add -A              # 把全部改动装进"待提交"
git commit -m "说明"     # 提交（写一个版本点）
git log --oneline       # 看历史提交记录
git tag                 # 看所有版本标签
git push                # 同步到 GitHub
git push --tags         # 同步标签到 GitHub
git checkout -- 文件名   # 撤销某个文件的改动（未提交前）
```

**心法**：改一点 → 提交一点。提交是免费的，后悔药。

---

## 六、GitHub 同步机制

- 本地「工作台」文件夹 = 你的 GitHub 仓库 `life-workbench` 的镜像。
- `git push` 之后，GitHub 上的代码、提交历史、更新日志、版本标签就和本地完全一致。
- 每个版本的 APK 通过 **GitHub Releases** 发布，别人（或你换手机时）可以从那里下载任何历史版本。
- 仓库默认**私有**，只有你能看。想开源给别人用，仓库 Settings → 最下面 Danger Zone → Change visibility → Public。

---

## 七、数据兼容性约定（重要）

老用户覆盖安装后，旧数据必须能用。因此：

- 改数据结构（比如给记录加字段）时，读取端要兼容旧格式（字段缺失给默认值）。
- 参考现有做法：`Demo.migrate()` 用 `demo_version` 做数据版本迁移。
- **绝不在迁移代码里静默删除用户真实数据。**

---

## 八、下一步路线（v2.1 / v3.0 候选）

- [ ] 底部导航改版（8 tab 收敛为 5 个一级入口）
- [ ] 深色模式（跟随系统）
- [ ] 记录编辑能力（账目/日程/习惯改名）
- [ ] 本地通知提醒（日程、习惯晚间提醒）
- [ ] 购物「已买」一键转记账
- [ ] 封面渐变改预置色板
- [ ] 桌面小组件（今日待办 + 一键打卡）
